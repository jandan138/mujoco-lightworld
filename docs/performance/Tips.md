# 性能与稳定性建议

采样与更新：
- 减少 `steps_per_epoch` 可降低显存占用，提升迭代频率；
- 适度增大 `train_iters` 保持每批次充分更新。

超参：
- `clip_ratio` 与 `target_kl` 控制策略更新幅度，过大易不稳定；
- `z_dim` 过大可能过拟合、过小可能表达不足。

辅助奖励：
- `alpha` 过大易抑制探索；推荐从 0.01 起做网格搜索。

工程建议：
- 定期保存模型与 CSV；避免意外中断导致数据丢失。