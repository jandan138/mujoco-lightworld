# 世界模型与 PPO 的结合

世界模型（WM）：
- Encoder：将原始观测压缩为潜空间特征 `z`（低维、结构化）。
- Dynamics：在潜空间上进行一步预测 `z_t → z_{t+1}`。

两种接入方式：
- Feature 模式：用 `z` 作为 PPO 的 Actor/Critic 输入，提升表征质量。
- Reward 模式：用预测误差作为辅助奖励（一般为惩罚项），让训练信号更丰富。

关键超参：
- `z_dim`：潜空间维度，影响特征表达能力与训练稳定性。
- `alpha`：辅助奖励权重，平衡误差惩罚与环境奖励。