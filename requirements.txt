# 依赖清单
# 说明：可通过 `pip install -r requirements.txt` 安装，建议先按 PyTorch 官网选择合适的 GPU/CPU 版本。

# 深度学习框架（请根据设备选择具体版本来源，例如 CUDA 12.1 的官方索引）
torch

# 强化学习环境（MuJoCo 连续控制），Gymnasium 提供 Walker2d/HalfCheetah 等任务
gymnasium[mujoco]

# 科学计算与数据处理
numpy
pandas

# 绘图与可视化
matplotlib

# 日志与可视化（可选）
tensorboard

# t-SNE 等可视化方法（E4 实验）
scikit-learn

# 进度条
tqdm

# 读取 YAML 配置
pyyaml